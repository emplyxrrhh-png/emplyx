name: Deploy React App to Azure

on:
  push:
    branches:
      - main
    paths:
      - 'Emplyx.React/**'
      - '.github/workflows/deploy-react.yml'

permissions:
  id-token: write
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Azure Login
      uses: azure/login@v2
      with:
        client-id: ${{ secrets.AZURE_CLIENT_ID }}
        tenant-id: ${{ secrets.AZURE_TENANT_ID }}
        subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '24'
        cache: 'npm'
        cache-dependency-path: Emplyx.React/package-lock.json
    
    - name: Install dependencies
      working-directory: ./Emplyx.React
      run: npm ci
    
    - name: Build React app
      working-directory: ./Emplyx.React
      run: npm run build
      env:
        VITE_API_URL: https://emplyxapi.azurewebsites.net/api
    
    - name: Install production dependencies for server
      working-directory: ./Emplyx.React/deploy-server
      run: npm ci --omit=dev
    
    - name: Copy dist to deploy-server
      run: |
        cp -r Emplyx.React/dist Emplyx.React/deploy-server/
    
    - name: Create deployment package
      working-directory: ./Emplyx.React/deploy-server
      run: |
        zip -r ../../react-server.zip .
    
    - name: Deploy to Azure Web App
      uses: azure/webapps-deploy@v3
      with:
        app-name: 'emplyx'
        slot-name: 'production'
        package: ./react-server.zip
    
    - name: Azure Logout
      run: az logout
      if: always()
