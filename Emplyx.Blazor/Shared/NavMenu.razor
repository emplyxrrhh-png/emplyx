@inject Microsoft.Extensions.Localization.IStringLocalizer<Emplyx.Blazor.Resources.SharedResources> T
@inject Emplyx.Blazor.Services.Features.IFeatureFlags Flags
@inject Emplyx.Blazor.Services.TenantContextState TenantContextState

<div class="nav-layout @(Collapsed ? "collapsed" : "")">
    <div class="company-section">
        <div class="company-card" role="button" @onclick="ToggleCompanyDropdown" title="@_currentCompany.Name">
            <div class="company-avatar">
                <i class="bi bi-building"></i>
            </div>
            @if (!Collapsed)
            {
                <div class="company-info">
                    <span class="company-name">@_currentCompany.Name</span>
                    <span class="company-plan">@_currentCompany.Plan</span>
                </div>
                <i class="bi bi-chevron-expand company-selector-icon"></i>
            }
        </div>

        @if (_isCompanyDropdownOpen && !Collapsed)
        {
            <div class="company-dropdown">
                @foreach (var company in _availableCompanies)
                {
                    <div class="company-dropdown-item" @onclick="() => SelectCompany(company)">
                        <div class="company-avatar small">
                            <i class="bi bi-building"></i>
                        </div>
                        <div class="company-info">
                            <span class="company-name">@company.Name</span>
                            <span class="company-plan">@company.Plan</span>
                        </div>
                        @if (company.Id == _currentCompany.Id)
                        {
                            <i class="bi bi-check2 text-primary"></i>
                        }
                    </div>
                }
            </div>
        }
    </div>

    <nav class="nav-scroll">
        <ul class="nav-list">
            @foreach (var item in _navItems)
            {
                if (!IsVisible(item)) { continue; }
                
                @if (item.Children != null && item.Children.Any())
                {
                    <li class="nav-group">
                        <div class="nav-group-header" title="@item.Label">
                            <span class="nav-icon"><i class="bi @item.Icon"></i></span>
                            @if (!Collapsed)
                            {
                                <span class="nav-text">@item.Label</span>
                            }
                        </div>
                        @if (!Collapsed)
                        {
                            <ul class="nav-group-items">
                                @foreach (var child in item.Children)
                                {
                                    <li class="nav-item">
                                        <NavLink class="nav-link" href="@child.Href" Match="child.Match">
                                            <span class="nav-icon"><i class="bi @child.Icon"></i></span>
                                            <span class="nav-text">@child.Label</span>
                                            @if (!string.IsNullOrWhiteSpace(child.Badge))
                                            {
                                                <span class="nav-badge">@child.Badge</span>
                                            }
                                        </NavLink>
                                    </li>
                                }
                            </ul>
                        }
                    </li>
                }
                else
                {
                    <li class="nav-item">
                        <NavLink class="nav-link" href="@item.Href" Match="item.Match" title="@item.Label">
                            <span class="nav-icon">
                                <i class="bi @item.Icon"></i>
                            </span>
                            @if (!Collapsed)
                            {
                                <span class="nav-text">@item.Label</span>
                                @if (!string.IsNullOrWhiteSpace(item.Badge))
                                {
                                    <span class="nav-badge">@item.Badge</span>
                                }
                            }
                        </NavLink>
                    </li>
                }
            }
        </ul>
    </nav>

    <div class="user-section">
        <div class="user-card" title="Usuario">
            <div class="user-avatar">
                <span>U</span>
            </div>
            @if (!Collapsed)
            {
                <div class="user-info">
                    <span class="user-name">Usuario</span>
                    <span class="user-role">Admin</span>
                </div>
                <button class="btn-logout" title="Cerrar sesión">
                    <i class="bi bi-box-arrow-right"></i>
                </button>
            }
        </div>
    </div>
</div>

@code {
    private sealed record NavItem(string Href, string Icon, string Label, List<NavItem>? Children = null, NavLinkMatch Match = NavLinkMatch.Prefix, string? Badge = null);

    private readonly IReadOnlyList<NavItem> _navItems = new List<NavItem>
    {
        new("/", "bi-house-door", "Inicio", null, NavLinkMatch.All),
        new("/organization", "bi-building", "Organización"),
        new("#", "bi-people", "Usuarios", new() { 
            new("/employees", "bi-person-badge", "Expediente de RRHH"), 
            new("/onboarding", "bi-person-plus", "OnBoarding"), 
            new("/requests", "bi-inbox", "Solicitudes"), 
            new("/surveys", "bi-clipboard-data", "Encuestas de RRHH") 
        }),
        new("#", "bi-calendar-week", "Gestión Horaria", new() { 
            new("/scheduler/calendar", "bi-calendar3", "Calendario"), 
            new("/absences/status", "bi-person-x", "Estado del absentismo") 
        }),
        new("/documents", "bi-folder2-open", "Gestor documental"),
        new("/zones", "bi-geo-alt", "Estado de las Zonas"),
        new("#", "bi-chat-left-text", "Comunicaciones", new() { 
            new("/communiques", "bi-megaphone", "Comunicados") 
        }),
        new("/bots", "bi-robot", "Bots"),
        new("#", "bi-graph-up", "Análisis, informes y datos", new() { 
            new("/genius", "bi-lightbulb", "Analytics by Genius"), 
            new("/reports", "bi-file-earmark-text", "Informes"), 
            new("/reports/queue", "bi-list-task", "Informes solicitados"), 
            new("/datalink", "bi-arrow-left-right", "Importar/Exportar") 
        }),
        new("#", "bi-cash-stack", "RealCost", new() { 
            new("/costcenters", "bi-wallet2", "Centros de coste") 
        }),
        new("/alerts", "bi-bell", "Alertas"),
        new("#", "bi-shield-check", "Seguridad y auditoría", new() { 
            new("/audit", "bi-eye", "Auditoría"), 
            new("/security/license", "bi-card-checklist", "Licencia"), 
            new("/security/lockdb", "bi-lock", "Fecha de cierre"), 
            new("/security/advanced", "bi-shield-lock", "Seguridad avanzada") 
        }),
        new("#", "bi-gear", "Configuración", new() { 
            new("/options/fields", "bi-input-cursor-text", "Ficha"), 
            new("/options/config", "bi-sliders", "Visualtime"), 
            new("/portal/config", "bi-layout-text-window-reverse", "Configuración del Portal"), 
            new("/security/password", "bi-key", "Contraseña"), 
            new("/options/emergency", "bi-exclamation-triangle", "Informe de emergencia"), 
            new("/sso", "bi-box-arrow-in-right", "Login integrado"), 
            new("/zones/config", "bi-map", "Zonas"), 
            new("/cameras", "bi-camera-video", "Cámaras"), 
            new("/notifications", "bi-bell-fill", "Notificaciones"), 
            new("/terminals", "bi-tablet", "Terminales") 
        }),
        new("#", "bi-gear-fill", "Configuración gestión horaria", new() { 
            new("/labagree", "bi-journal-text", "Convenios"), 
            new("/shifts", "bi-clock", "Horarios"), 
            new("/causes", "bi-chat-square-text", "Justificaciones"), 
            new("/concepts", "bi-calculator", "Saldos"), 
            new("/indicators", "bi-speedometer2", "Indicadores") 
        })
    };

    private bool _isCompanyDropdownOpen = false;

    private record CompanyInfo(string Id, string Name, string Plan);

    private readonly List<CompanyInfo> _availableCompanies = new()
    {
        new("1", "Emplyx S.L.", "Enterprise"),
        new("2", "StartUp Innovadora", "Basic")
    };

    private CompanyInfo _currentCompany = new("1", "Emplyx S.L.", "Enterprise");

    private void ToggleCompanyDropdown()
    {
        _isCompanyDropdownOpen = !_isCompanyDropdownOpen;
    }

    private void SelectCompany(CompanyInfo company)
    {
        _currentCompany = company;
        _isCompanyDropdownOpen = false;
    }

    private bool IsVisible(NavItem item)
    {
        // Simplified visibility logic for now, can be expanded
        return true;
    }

    [Parameter]
    public bool Collapsed { get; set; }
}
